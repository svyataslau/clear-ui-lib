{
  "buildCommand": "npm install --ignore-scripts && cd storybook && npm install && npm run build",
  "outputDirectory": "storybook/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
